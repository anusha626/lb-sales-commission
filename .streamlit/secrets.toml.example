# Copy this file to `.streamlit/secrets.toml` to enable the password gate
# locally. The real `secrets.toml` is in .gitignore and must NEVER be
# committed. On Streamlit Cloud, set the same value in the Secrets manager
# in the app's dashboard instead of a file.

app_password = "your-password-here"
